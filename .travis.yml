language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t marcelkooi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true marcelkooi/docker-react npm run test
